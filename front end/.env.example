# Browser-visible values are public. Never put API keys or provider secrets here.
# Existing API requests use same-origin /api. This value configures the development proxy only.
GREETO_API_TARGET=http://localhost:3001
# Optional PUBLIC endpoint used by existing webhook URL displays; no credentials.
VITE_PUBLIC_API_URL=
