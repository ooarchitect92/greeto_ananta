# QA evidence boundary

The TAP, JSON and command outputs record checks actually executed in this environment. Layout PNGs are **static CSS fixture screenshots**, not live React screenshots. `layout-fixture.html` is a development reference and is not linked into the product router. They must not be used as evidence of working messages, AI, provider connectivity, billing or full application behavior.

The production build failed to start because dependency installation was blocked. The supplied React browser tests remain pending execution after a clean supported install. See `../QUALITY_REPORT.md`.
