/** Stable feature facade over the original frontend API adapter. */
export { acceptWorkspaceInvite, adminLogin, customerLogin, registerWorkspace, requestPasswordReset, resendVerification, resetPassword, verifyEmail, verifyTwoFactor } from '../../services/api/legacy.js';
